---
const { title, subtitle } = Astro.props;
---
<section class="relative overflow-hidden bg-gradient-to-b from-primary/15 to-transparent">
  <div class="mx-auto max-w-7xl px-4 py-20 md:py-28 grid md:grid-cols-2 gap-10 items-center">
    <div>
      <h1 class="text-4xl md:text-5xl font-extrabold leading-tight">{title}</h1>
      <p class="mt-4 text-lg text-slate-700">{subtitle}</p>
      <div class="mt-8 flex gap-4">
        <a href="#servicios" class="rounded-xl bg-primary text-white px-5 py-3">Ver servicios</a>
        <a href="#contacto" class="rounded-xl border border-primary text-primary px-5 py-3">Contáctanos</a>
      </div>
    </div>
    <!-- Placeholder visual element; replace with a relevant image or illustration later. -->
    <div class="h-60 md:h-80 rounded-3xl bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-primary/30 to-primary/0"></div>
  </div>
</section>